rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product images: allow anyone to read, only authenticated users to upload
    match /products/{productId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
